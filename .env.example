# Pick one (Anthropic preferred). Without either, the pipeline falls back to
# an AST-based heuristic so end-to-end runs still work.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Optional overrides
KERNEL_SYNTH_MODEL=claude-sonnet-4-5
KERNEL_SYNTH_MAX_AGENT_STEPS=24
KERNEL_SYNTH_DATA_DIR=./data
